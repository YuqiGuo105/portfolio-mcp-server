name: Deploy portfolio-mcp-server

on:
  workflow_run:
    workflows: ["CI"]
    branches: [main]
    types: [completed]

  workflow_dispatch: {}

permissions:
  contents: read
  id-token: write

concurrency:
  group: deploy-portfolio-mcp-server
  cancel-in-progress: false

env:
  PROJECT_ID: portfolio-notify-prod
  REGION: us-central1
  ARTIFACT_REPO: portfolio
  IMAGE_NAME: portfolio-mcp-server
  SERVICE_NAME: portfolio-mcp-server
  RUNTIME_SA_EMAIL: admin-platform-runtime@portfolio-notify-prod.iam.gserviceaccount.com
  DEPLOY_SHA: ${{ github.event.workflow_run.head_sha || github.sha }}

jobs:
  deploy:
    if: >
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout CI-approved commit
        uses: actions/checkout@v4
        with:
          ref: ${{ env.DEPLOY_SHA }}

      - name: Compute image tag
        id: tag
        run: |
          TAG="sha-$(echo "${DEPLOY_SHA}" | cut -c1-7)"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Authenticate to Google Cloud (Workload Identity Federation)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.DEPLOYER_SA_EMAIL }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${REGION}-docker.pkg.dev --quiet

      - name: Build & push image
        run: |
          IMAGE="${REGION}-docker.pkg.dev/${PROJECT_ID}/${ARTIFACT_REPO}/${IMAGE_NAME}:${{ steps.tag.outputs.tag }}"
          docker build -t "${IMAGE}" .
          docker push "${IMAGE}"
          echo "IMAGE=${IMAGE}" >> "$GITHUB_ENV"

      - name: Deploy to Cloud Run (budget-optimized)
        run: |
          gcloud run deploy "${SERVICE_NAME}" \
            --image="${IMAGE}" \
            --region="${REGION}" \
            --project="${PROJECT_ID}" \
            --service-account="${RUNTIME_SA_EMAIL}" \
            --allow-unauthenticated \
            --port=8080 \
            --min-instances=0 \
            --max-instances=1 \
            --memory=256Mi \
            --cpu=1 \
            --cpu-boost \
            --cpu-throttling \
            --timeout=60 \
            --concurrency=80 \
            --set-env-vars="NODE_ENV=production,MCP_GATEWAY_URL=${{ vars.MCP_GATEWAY_URL }},SITE_URL=${{ vars.SITE_URL }},MCP_SERVER_PUBLIC_URL=https://portfolio-mcp-server-702193211434.us-central1.run.app,MAX_CONTENT_LENGTH=${{ vars.MAX_CONTENT_LENGTH }},ADMIN_ALLOWED_EMAILS=${{ vars.ADMIN_ALLOWED_EMAILS }},ADMIN_SERVICE_URL=${{ vars.ADMIN_SERVICE_URL || 'https://portfolio-admin-service-702193211434.us-central1.run.app' }},SUPABASE_AUTH_ISSUER=https://iyvhmpdfrnznxgyvvkvx.supabase.co/auth/v1" \
            --update-secrets="MCP_GATEWAY_INTERNAL_TOKEN=MCP_GATEWAY_INTERNAL_TOKEN:latest,SUPABASE_JWT_SECRET=SUPABASE_JWT_SECRET:latest,ADMIN_SERVICE_INTERNAL_TOKEN=INTERNAL_API_TOKEN:latest"

      - name: Smoke test
        run: |
          URL=$(gcloud run services describe "${SERVICE_NAME}" \
            --region="${REGION}" --project="${PROJECT_ID}" \
            --format='value(status.url)')
          for i in $(seq 1 20); do
            if curl -sf "${URL}/health" | grep -q '"ok"' && \
               curl -sf "${URL}/.well-known/oauth-protected-resource/mcp/admin" | grep -q 'authorization_servers'; then
              echo "Health and OAuth discovery checks passed: ${URL}"
              exit 0
            fi
            sleep 3
          done
          echo "Smoke test failed"
          exit 1
